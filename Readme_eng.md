# binds on DDNet (You need to move them to %appdata%/DDNet, type this in Win + R)
1. reset switch (replace the key with your own) default “j”
2. deep fly (replace the key with your own) default “x”
3. copy deep fly (replace the key with your own) default “e”
